const config = {
    dataFolder: "./data",
    files: {
        classes: "classes.json",
        faculties: "faculties.json",
        subjects: "subjects.json",
        config: "config.json",
        timeTableFolder: "timetable",
        timetableFile: "timetable.json"
    }
}


export default config